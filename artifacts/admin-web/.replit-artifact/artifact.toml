kind = "web"
previewPath = "/"
title = "Floaters CONNECT Admin Console"
description = "A secure admin console for managing Floaters CONNECT users, disputes, and platform tags."
version = "1.0.0"
id = "artifacts/admin-web"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24276

[services.development]
run = "pnpm --filter @workspace/admin-web run dev"

[services.production]

[services.production.build]
args = [ "pnpm", "--filter", "@workspace/admin-web", "run", "build" ]

[services.production.build.env]
NODE_ENV = "production"

[services.production.run]
args = [ "pnpm", "--filter", "@workspace/admin-web", "run", "start" ]

[services.production.run.env]
PORT = "24276"
NODE_ENV = "production"

[services.production.health.startup]
path = "/"

[services.env]
PORT = "24276"
BASE_PATH = "/"